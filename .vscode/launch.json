{
 "version": "0.2.0",
 "configurations": [
  {
   "type": "msedge",
   "request": "launch",
   "name": "localhost に対して Edge を起動する",
   "url": "http://localhost:8080",
   "webRoot": "${workspaceFolder}"
  },
 ]
}